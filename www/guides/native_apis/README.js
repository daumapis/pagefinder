Ext.data.JsonP.native_apis({"guide":"<h1 id='native_apis-section-accessing-native-apis-with-ext-device'>Accessing Native APIs with Ext.device</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/native_apis-section-getting-started'>Getting Started</a></li>\n<li><a href='#!/guide/native_apis-section-connection'>Connection</a></li>\n<li><a href='#!/guide/native_apis-section-notification'>Notification</a></li>\n<li><a href='#!/guide/native_apis-section-camera'>Camera</a></li>\n<li><a href='#!/guide/native_apis-section-orientation'>Orientation</a></li>\n<li><a href='#!/guide/native_apis-section-custom-scheme-urls'>Custom Scheme URLs</a></li>\n<li><a href='#!/guide/native_apis-section-packaging'>Packaging</a></li>\n</ol>\n</div>\n\n<p>One of the most important benefits of native packaging is the access to native APIs. Sencha Touch provides an access to native APIs through its <code>Ext.device</code> API. The <code>Ext.device</code> API does not itself provide the native API capabilities, it is just a wrapper that can be used to access native APIs powered by the Sencha Packager or third party solutions, such as PhoneGap and the iOS Simulator. At the time of writing, <code>Ext.device</code> provides the following four APIs:</p>\n\n<ul>\n<li><a href=\"#!/api/Ext.device.Connection\" rel=\"Ext.device.Connection\" class=\"docClass\">Ext.device.Connection</a> - indicates whether the device has a current connection to the internet.</li>\n<li><a href=\"#!/api/Ext.device.Notification\" rel=\"Ext.device.Notification\" class=\"docClass\">Ext.device.Notification</a> - enables you to use native notification windows (the native version of <a href=\"#!/api/Ext.Msg\" rel=\"Ext.Msg\" class=\"docClass\">Ext.Msg</a>).</li>\n<li><a href=\"#!/api/Ext.device.Orientation\" rel=\"Ext.device.Orientation\" class=\"docClass\">Ext.device.Orientation</a> - provides information on the device's current orientation.</li>\n<li><a href=\"#!/api/Ext.device.Camera\" rel=\"Ext.device.Camera\" class=\"docClass\">Ext.device.Camera</a> - allows your app to take pictures or select images from the camera library (with the user's permission).</li>\n</ul>\n\n\n<h2 id='native_apis-section-getting-started'>Getting Started</h2>\n\n<p>Because we want to keep our apps small, and because native APIs only apply in native packaged apps, the <code>Ext.device</code> API is not included in the standard build of Sencha Touch. When you want to use any of the Device APIs , you must require them using <code><a href=\"#!/api/Ext-method-require\" rel=\"Ext-method-require\" class=\"docClass\">Ext.require</a></code>:</p>\n\n<pre><code><a href=\"#!/api/Ext-method-require\" rel=\"Ext-method-require\" class=\"docClass\">Ext.require</a>('<a href=\"#!/api/Ext.device.Connection\" rel=\"Ext.device.Connection\" class=\"docClass\">Ext.device.Connection</a>');\n\n<a href=\"#!/api/Ext-method-application\" rel=\"Ext-method-application\" class=\"docClass\">Ext.application</a>({\n    name: 'MyApp'\n    // the rest of your app follows\n});\n</code></pre>\n\n<h2 id='native_apis-section-connection'>Connection</h2>\n\n<p>The Connection API is used to determine whether the device is currently online. It exposes two main methods - <code>isOnline</code> and <code>getType</code>:</p>\n\n<pre><code>if (<a href=\"#!/api/Ext.device.Connection-method-isOnline\" rel=\"Ext.device.Connection-method-isOnline\" class=\"docClass\">Ext.device.Connection.isOnline</a>()) {\n    <a href=\"#!/api/Ext.Msg-method-alert\" rel=\"Ext.Msg-method-alert\" class=\"docClass\">Ext.Msg.alert</a>('You are currently connected via ' + <a href=\"#!/api/Ext.device.Connection-method-getType\" rel=\"Ext.device.Connection-method-getType\" class=\"docClass\">Ext.device.Connection.getType</a>());\n} else {\n    <a href=\"#!/api/Ext.Msg-method-alert\" rel=\"Ext.Msg-method-alert\" class=\"docClass\">Ext.Msg.alert</a>('You are not currently connected');\n}\n</code></pre>\n\n<p>This is probably the simplest of the APIs offered by <code>Ext.device</code>. If you load this code on a device or inside the simulator, you will see a Sencha Touch message box appearing, indicating that you are offline, or indicating your connection type (wifi, 2g, 3g, and so on), if you are online. See the <a href=\"#!/api/Ext.device.Connection\" rel=\"Ext.device.Connection\" class=\"docClass\">Ext.device.Connection</a> docs for a full list of possible connection types.</p>\n\n<h2 id='native_apis-section-notification'>Notification</h2>\n\n<p>Notifications are used to show native dialog boxes. Notifications work very much like <a href=\"#!/api/Ext.Msg\" rel=\"Ext.Msg\" class=\"docClass\">Ext.Msg</a> and can be configured in many different ways. For example, the following example shows how to display a native notification box with a title and a message:</p>\n\n<pre><code><a href=\"#!/api/Ext.device.Notification-method-show\" rel=\"Ext.device.Notification-method-show\" class=\"docClass\">Ext.device.Notification.show</a>({\n    title: 'One Button',\n    message: 'This is a simple notification with one button.'\n});\n</code></pre>\n\n<p>This code causes a native notification window to appear as shown in the following image:</p>\n\n<p><p class='screenshot'><img src='guides/native_apis/notifications-simple.png' alt=''><span></span></p></p>\n\n<p>The following code displays a native message box with the configured title and message. It also adds an OK button, so the user can dismiss the message. When the user taps on the button, the callback is called - in this example we respond to the button tap by opening a second notification:</p>\n\n<pre><code><a href=\"#!/api/Ext.device.Notification-method-show\" rel=\"Ext.device.Notification-method-show\" class=\"docClass\">Ext.device.Notification.show</a>({\n    title: 'One Button',\n    message: 'This is a simple notification with one button.',\n\n    callback: function(button) {\n        //When the user taps a button, show another notification\n        <a href=\"#!/api/Ext.device.Notification-method-show\" rel=\"Ext.device.Notification-method-show\" class=\"docClass\">Ext.device.Notification.show</a>({\n            message: 'You pressed: \"' + button + '\"'\n        });\n    }\n});\n</code></pre>\n\n<p>We did not provide a title for the second message, which is fine - it just shows the message and an OK button. We can customize the buttons that are presented to the user as follows:</p>\n\n<pre><code><a href=\"#!/api/Ext.device.Notification-method-show\" rel=\"Ext.device.Notification-method-show\" class=\"docClass\">Ext.device.Notification.show</a>({\n    title: 'Multiple Buttons',\n    message: 'This is a notification with multiple buttons.',\n    buttons: [\"Cancel\", \"Login\", \"Another\"],\n\n    callback: function(button) {\n        //When the user taps a button, show another notification\n        <a href=\"#!/api/Ext.device.Notification-method-show\" rel=\"Ext.device.Notification-method-show\" class=\"docClass\">Ext.device.Notification.show</a>({\n            message: 'You pressed: \"' + button + '\"'\n        });\n    }\n});\n</code></pre>\n\n<p>This time we provided three button text strings in the <em>buttons</em> array - when we run this code we now see the buttons stacked on top of one another as shown by the following image:</p>\n\n<p><p class='screenshot'><img src='guides/native_apis/notifications-custom.png' alt=''><span></span></p></p>\n\n<h3 id='native_apis-section-vibration'>Vibration</h3>\n\n<p>Another capability of the notification API is to vibrate the device. You can make this happen as shown in the following code:</p>\n\n<pre><code>Ext.device.notification.vibrate();\n</code></pre>\n\n<p>This method call causes the device to vibrate for a short time, and can be called as often as desired.</p>\n\n<h2 id='native_apis-section-camera'>Camera</h2>\n\n<p>The Camera API allows you to take pictures using the phone's camera, or alternatively select an image from the existing photo library available on the phone. The following code sample shows how to take a new picture:</p>\n\n<pre><code><a href=\"#!/api/Ext.device.Camera-method-capture\" rel=\"Ext.device.Camera-method-capture\" class=\"docClass\">Ext.device.Camera.capture</a>({\n    source: 'camera',\n    destination: 'file',\n\n    success: function(url) {\n        //show the newly captured image in a full screen <a href=\"#!/api/Ext.Img\" rel=\"Ext.Img\" class=\"docClass\">Ext.Img</a> component:\n        <a href=\"#!/api/Ext-method-create\" rel=\"Ext-method-create\" class=\"docClass\">Ext.create</a>('<a href=\"#!/api/Ext.Img\" rel=\"Ext.Img\" class=\"docClass\">Ext.Img</a>', {\n            src: url,\n            fullscreen: true\n        });\n    }\n});\n</code></pre>\n\n<p>Note that we specified two options (apart from our <em>success</em> callback) - source and destination. We can set <em>source</em> to either 'camera' or 'library' - the first option opens the camera to take a new picture, the second opens a native photo browser window to select an existing image. This example opens the camera app then saves the new image to a file, finally calling our <em>success</em> function with the local url to that file.</p>\n\n<p>The other options that can be passed into the <a href=\"#!/api/Ext.device.Camera-method-capture\" rel=\"Ext.device.Camera-method-capture\" class=\"docClass\">Ext.device.Camera.capture</a> call are the following:</p>\n\n<ul>\n<li><code>success</code> - the function to call if an image was successfully acquired.</li>\n<li><code>failure</code> - the function to call if an image could not be acquired.</li>\n<li><code>scope</code> - the scope to run the two previously mentioned functions in (sets the value of <code>this</code>).</li>\n<li><code>quality</code> - a hint provided to the camera about the compression quality of the image (0-100).</li>\n<li><code>width</code> - the desired width of the new image, in px.</li>\n<li><code>height</code> - the desired height of the new image, in px.</li>\n<li><code>source</code> - can be 'camera' or 'library' - 'camera' takes a new picture, 'library' chooses an existing one.</li>\n<li><code>destination</code> - can be 'file' or 'data' - file returns a local url to the new image, 'data' returns a Base64 encoded string of the image.</li>\n<li><code>encoding</code> - can be 'jpeg', 'jpg' or 'png' - the first two options encode the image as a JPEG, the third as a PNG.</li>\n</ul>\n\n\n<p>Any number of options can be passed into your <a href=\"#!/api/Ext.device.Camera-method-capture\" rel=\"Ext.device.Camera-method-capture\" class=\"docClass\">Ext.device.Camera.capture</a> call.</p>\n\n<h2 id='native_apis-section-orientation'>Orientation</h2>\n\n<p>The orientation API yields information about the current orientation of the device. In the following example we register a handler function that is called every time a device orientation change is detected:</p>\n\n<pre><code><a href=\"#!/api/Ext.device.Orientation-method-on\" rel=\"Ext.device.Orientation-method-on\" class=\"docClass\">Ext.device.Orientation.on</a>('orientation', function(e) {\n    var alpha = Math.round(e.alpha),\n        beta = Math.round(e.beta),\n        gamma = Math.round(e.gamma);\n\n    console.log(alpha, beta, gamma);\n});\n</code></pre>\n\n<p>This example provides a stream of console messages with the alpha, beta, and gamma orientation of the device. These values correspond to the three dimensions the device can be pivoted around, and are expressed in degrees ranging from 0 to 360.</p>\n\n<h2 id='native_apis-section-custom-scheme-urls'>Custom Scheme URLs</h2>\n\n<p>You can use custom scheme URLs to launch an application from other applications:</p>\n\n<pre><code><a href=\"#!/api/Ext-method-application\" rel=\"Ext-method-application\" class=\"docClass\">Ext.application</a>({\n    name: 'Sencha',\n    requires: ['<a href=\"#!/api/Ext.device.Device\" rel=\"Ext.device.Device\" class=\"docClass\">Ext.device.Device</a>'],\n    launch: function() {\n        if (<a href=\"#!/api/Ext.device.Device-property-scheme\" rel=\"Ext.device.Device-property-scheme\" class=\"docClass\">Ext.device.Device.scheme</a>) {\n            // the application was opened via another application. Do something:\n            alert('Applicaton opened via another application: ' + Ext.device.Device.scheme.url);\n        }\n\n        // Listen for future changes\n        <a href=\"#!/api/Ext.device.Device-method-on\" rel=\"Ext.device.Device-method-on\" class=\"docClass\">Ext.device.Device.on</a>('schemeupdate', function(device, scheme) {\n            // the application was launched, closed, and then launched another from another application\n            // this means onReady will not be called again (because the application is already running  \n            // in the background) - but this event will be fired\n            alert('Applicated reopened via another application: ' + scheme.url);\n        }, this);\n    }\n});\n</code></pre>\n\n<p>In order for this code to work, when packaging your application you must add add the custom URLs you would like to use. You can do this by adding the following code into the <code>rawConfig</code> property inside your <code>package.json</code> file (Sencha Native Packager configuration file):</p>\n\n<pre><code>{\n    ...\n    \"rawConfig\": \"&lt;key&gt;CFBundleURLTypes&lt;/key&gt;&lt;array&gt;&lt;dict&gt;&lt;key&gt;CFBundleURLSchemes&lt;/key&gt;&lt;array&gt;&lt;string&gt;sencha&lt;/string&gt;&lt;/array&gt;&lt;key&gt;CFBundleURLName&lt;/key&gt;&lt;string&gt;com.sencha.example&lt;/string&gt;&lt;/dict&gt;&lt;/array&gt;\"\n    ...\n}\n</code></pre>\n\n<p>You can change the available URL schemes and the application identifier from the previous example.</p>\n\n<p>You can then test the code by packaging and installing the application onto a device/iOS Simulator,\nopening Safari and typing: <code>sencha:testing</code>. The application will launch and it will <code>alert</code> the specified URL.</p>\n\n<p><strong>Note:</strong> This currently only works with the Sencha Native Packager. If you attempt to listen\nto this event when packaged with PhoneGap or simply in the browser, it will not function.</p>\n\n<h2 id='native_apis-section-packaging'>Packaging</h2>\n\n<p>Now that you have an understanding of how to use the <code>Ext.device</code> API to access\nnative device functionality, proceed to the guides on packaging your app\nfor <a href=\"#!/guide/native_packaging\">iOS and other devices</a> and <a href=\"#!/guide/native_android\">Android</a> to\nstart testing the app on a real device.</p>\n","title":"Using Native APIs"});