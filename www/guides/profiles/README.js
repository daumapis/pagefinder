Ext.data.JsonP.profiles({"guide":"<h1 id='profiles-section-using-device-profiles'>Using Device Profiles</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/profiles-section-setting-up-profiles'>Setting Up Profiles</a></li>\n<li><a href='#!/guide/profiles-section-determining-the-active-profile'>Determining the Active Profile</a></li>\n<li><a href='#!/guide/profiles-section-the-launch-process'>The Launch Process</a></li>\n<li><a href='#!/guide/profiles-section-specializing-views'>Specializing Views</a></li>\n<li><a href='#!/guide/profiles-section-specializing-controllers'>Specializing Controllers</a></li>\n<li><a href='#!/guide/profiles-section-specializing-models'>Specializing Models</a></li>\n</ol>\n</div>\n\n<p>Today's mobile web applications are expected to work on a wide variety of devices, spanning from the smallest mobile phones to the largest tablets. These devices have a wide range of screen resolutions and are used in different ways. People tend to use phone apps while out of the house to rapidly gather some information or to perform some action - often in a very short time. Tablet apps are more likely to be used for longer periods of time, usually within the home or somewhere else where they can sit for a long time.</p>\n\n<p>All of this means that people expect different app experiences on different devices. However, much of your application logic and assets can be shared between the different experiences. Writing separate apps for each platform is time consuming, error-prone and plain boring. Thankfully, Device Profiles give us a simple way to share  between device types as much code as needed, while making it easy to customize behavior, appearance, and workflows for each device.</p>\n\n<h2 id='profiles-section-setting-up-profiles'>Setting Up Profiles</h2>\n\n<p>Device Profiles exist within the context of an <a href=\"#!/api/Ext.app.Application\" rel=\"Ext.app.Application\" class=\"docClass\">Application</a>, for example if we wanted to create an email app with phone and tablet profiles, we could define our app.js shown in the following code sample (see the <a href=\"#!/guide/apps_intro\">Intro to Apps guide</a> if this is not familiar):</p>\n\n<pre><code><a href=\"#!/api/Ext-method-application\" rel=\"Ext-method-application\" class=\"docClass\">Ext.application</a>({\n    name: 'Mail',\n\n    profiles: ['Phone', 'Tablet']\n});\n</code></pre>\n\n<p>Since we did not give our Application a <a href=\"#!/api/Ext.app.Application-cfg-launch\" rel=\"Ext.app.Application-cfg-launch\" class=\"docClass\">launch function</a>, at the moment it is only going to load those two Profiles. By convention, it expects to find them in <em>app/profile/Phone.js</em> and <em>app/profile/Tablet.js</em>. The following code shows the Phone profile:</p>\n\n<pre><code><a href=\"#!/api/Ext-method-define\" rel=\"Ext-method-define\" class=\"docClass\">Ext.define</a>('Mail.profile.Phone', {\n    extend: '<a href=\"#!/api/Ext.app.Profile\" rel=\"Ext.app.Profile\" class=\"docClass\">Ext.app.Profile</a>',\n\n    config: {\n        name: 'Phone',\n        views: ['Main']\n    },\n\n    isActive: function() {\n        return <a href=\"#!/api/Ext.os-method-is\" rel=\"Ext.os-method-is\" class=\"docClass\">Ext.os.is</a>('Phone');\n    }\n});\n</code></pre>\n\n<p>The Tablet profile follows the same pattern. In the Phone profile we only supplied three pieces of information - the Profile <a href=\"#!/api/Ext.app.Profile-cfg-name\" rel=\"Ext.app.Profile-cfg-name\" class=\"docClass\">name</a>, the optional set of additional <a href=\"#!/api/Ext.app.Profile-cfg-views\" rel=\"Ext.app.Profile-cfg-views\" class=\"docClass\">views</a> to load when this Profile is activated, and an <a href=\"#!/api/Ext.app.Profile-method-isActive\" rel=\"Ext.app.Profile-method-isActive\" class=\"docClass\">isActive</a> function.</p>\n\n<p>The <a href=\"#!/api/Ext.app.Profile-method-isActive\" rel=\"Ext.app.Profile-method-isActive\" class=\"docClass\">isActive</a> function determines if a given profile should be active on the device your app runs on. By far the most common approach is to create profiles for Phone and Tablet, using the built-in <code><a href=\"#!/api/Ext.os-method-is\" rel=\"Ext.os-method-is\" class=\"docClass\">Ext.os.is</a></code> method (e.g. <a href=\"#!/api/Ext.os-method-is\" rel=\"Ext.os-method-is\" class=\"docClass\">Ext.os.is</a>(<code>Phone</code>). You can write any code you like in the <code>isActive</code> function, as long as it always returns <code>true</code> or <code>false</code> for the device it is running on.</p>\n\n<h2 id='profiles-section-determining-the-active-profile'>Determining the Active Profile</h2>\n\n<p>Once the Profiles have been loaded, their isActive functions are called in turn. The first one that return true is the Profile that the Application boots with. This Profile is then set to the Application's <a href=\"#!/api/Ext.app.Application-cfg-currentProfile\" rel=\"Ext.app.Application-cfg-currentProfile\" class=\"docClass\">currentProfile</a>, and the Application prepares to load all of its dependencies - the models, views, controllers, and other classes that constitute the app. It does this by combining its own dependencies with those specified in the active profile.</p>\n\n<p>For example, let us amend our previous Application so that it loads its own Models and Views:</p>\n\n<pre><code><a href=\"#!/api/Ext-method-application\" rel=\"Ext-method-application\" class=\"docClass\">Ext.application</a>({\n    name: 'Mail',\n\n    profiles: ['Phone', 'Tablet'],\n\n    models: ['User'],\n    views: ['Navigation', 'Login']\n});\n</code></pre>\n\n<p>Finally, when we load the app on a phone, the Phone profile is activated and the application loads the following files:</p>\n\n<ul>\n<li>app/model/User.js</li>\n<li>app/view/Navigation.js</li>\n<li>app/view/Login.js</li>\n<li>app/view/<strong>phone</strong>/Main.js</li>\n</ul>\n\n\n<p>The first three items are specified in the Application itself - the User model plus the Navigation and Login views. The fourth item is specified by the Phone Profile and follows a special form. By convention, classes that are specific to a Profile are expected to be defined in a subdirectory with the same name as the Profile. For example, the 'Main' view specified in the Phone Profile is loaded from <em>app/view/<strong>phone</strong>/Main.js</em>, whereas if we had defined 'Main' in the Application, it would be loaded from <em>app/view/Main.js</em>.</p>\n\n<p>The same applies to all models, views, controllers, and stores loaded in a Profile. This is important as it enables you to easily share behavior, view logic, and more between profiles (see the specializing views and controllers sections below). If you need to load classes that do not comply with this convention, you can specify the full class name instead:</p>\n\n<pre><code><a href=\"#!/api/Ext-method-define\" rel=\"Ext-method-define\" class=\"docClass\">Ext.define</a>('Mail.profile.Phone', {\n    extend: '<a href=\"#!/api/Ext.app.Profile\" rel=\"Ext.app.Profile\" class=\"docClass\">Ext.app.Profile</a>',\n\n    config: {\n        name: 'Phone',\n        views: ['Main', 'Mail.view.SpecialView'],\n        models: ['Mail.model.Message']\n    },\n\n    isActive: function() {\n        return <a href=\"#!/api/Ext.os-method-is\" rel=\"Ext.os-method-is\" class=\"docClass\">Ext.os.is</a>('Phone');\n    }\n});\n</code></pre>\n\n<p>As evident from this example, you can mix and match fully-qualified class names (e.g. <em>'Mail.view.SomeView'</em>) and relatively specified class names (e.g. <em>'Main'</em>, which becomes <em>'Mail.view.phone.Main'</em>). Be aware that all models, views, controllers, and stores specified for a Profile are treated this way. This means if there are Models or Stores that you want to load for Tablets only, but do not want to create classes like <em>Mail.model.tablet.User</em>, you should specify the fully-qualified class names instead (e.g. <em>Mail.model.User</em> in this case).</p>\n\n<h2 id='profiles-section-the-launch-process'>The Launch Process</h2>\n\n<p>The launch process using Profiles is almost exactly the same as that without using Profiles. Profile-based apps have a 3-stage launch process; after all of the dependencies have been loaded, the following happens:</p>\n\n<ol>\n<li>Controllers are instantiated; each Controller's <a href=\"#!/api/Ext.app.Controller-cfg-init\" rel=\"Ext.app.Controller-cfg-init\" class=\"docClass\">init</a> function is called</li>\n<li>The Profile's <a href=\"#!/api/Ext.app.Profile-method-launch\" rel=\"Ext.app.Profile-method-launch\" class=\"docClass\">launch</a> function is called</li>\n<li>The Application's <a href=\"#!/api/Ext.app.Application-cfg-launch\" rel=\"Ext.app.Application-cfg-launch\" class=\"docClass\">launch</a> function is called.</li>\n</ol>\n\n\n<p>When using Profiles it is common to use the Profile launch functions to create the app's initial UI. In many cases this means the Application's launch function is completely removed, as the initial UI is usually different in each Profile (you can still specify an Application-wide launch function for setting up items such as analytics or other profile-agnostic setup).</p>\n\n<p>A typical Profile launch function might look like in the following sample:</p>\n\n<pre><code><a href=\"#!/api/Ext-method-define\" rel=\"Ext-method-define\" class=\"docClass\">Ext.define</a>('Mail.profile.Phone', {\n    extend: '<a href=\"#!/api/Ext.app.Profile\" rel=\"Ext.app.Profile\" class=\"docClass\">Ext.app.Profile</a>',\n\n    config: {\n        name: 'Phone',\n        views: ['Main']\n    },\n\n    isActive: function() {\n        return <a href=\"#!/api/Ext.os-method-is\" rel=\"Ext.os-method-is\" class=\"docClass\">Ext.os.is</a>('Phone');\n    },\n\n    launch: function() {\n        <a href=\"#!/api/Ext-method-create\" rel=\"Ext-method-create\" class=\"docClass\">Ext.create</a>('Mail.view.phone.Main');\n    }\n});\n</code></pre>\n\n<p>Note that both Profile and Application launch functions are optional - if you do not define them, they are not called.</p>\n\n<h2 id='profiles-section-specializing-views'>Specializing Views</h2>\n\n<p>Most of the specialization implemented by a Profile occurs in the Views and the Controllers. Let us assume that we have the following tablet Profile:</p>\n\n<pre><code><a href=\"#!/api/Ext-method-define\" rel=\"Ext-method-define\" class=\"docClass\">Ext.define</a>('Mail.profile.Tablet', {\n    extend: '<a href=\"#!/api/Ext.app.Profile\" rel=\"Ext.app.Profile\" class=\"docClass\">Ext.app.Profile</a>',\n\n    config: {\n        views: ['Main']\n    },\n\n    launch: function() {\n        <a href=\"#!/api/Ext-method-create\" rel=\"Ext-method-create\" class=\"docClass\">Ext.create</a>('Mail.view.tablet.Main');\n    }\n});\n</code></pre>\n\n<p>When this app boots on a tablet device, the file <em>app/views/tablet/Main.js</em> is loaded as usual. The following code sample is the content of the <em>app/views/tablet/Main.js</em> file:</p>\n\n<pre><code><a href=\"#!/api/Ext-method-define\" rel=\"Ext-method-define\" class=\"docClass\">Ext.define</a>('Mail.view.tablet.Main', {\n    extend: 'Mail.view.Main',\n\n    config: {\n        title: 'Tablet-specific version'\n    }\n});\n</code></pre>\n\n<p>Usually, when we define a view class we extend one of Sencha Touch's built in views, but in the previous example we extend Mail.view.Main, one of our own views. The following code sample illustrates how Mail.view.Main looks like:</p>\n\n<pre><code><a href=\"#!/api/Ext-method-define\" rel=\"Ext-method-define\" class=\"docClass\">Ext.define</a>('Mail.view.Main', {\n    extend: '<a href=\"#!/api/Ext.Panel\" rel=\"Ext.Panel\" class=\"docClass\">Ext.Panel</a>',\n\n    config: {\n        title: 'Generic version',\n        html: 'This is the main screen'\n    }\n});\n</code></pre>\n\n<p>We thus have a superclass (Mail.view.Main) and a Profile-specific subclass (Mail.view.tablet.Main), which can customize any aspect of the superclass. In this case we change the title of the Main view from \"Generic version\" to \"Tablet-specific version\" in our subclass, so that is what we see when the app loads.</p>\n\n<p>Since these are normal classes, it is easy to customize almost any part of the superclass using the flexible config system. For example, assuming that we also have a phone version of the app, we could customize its version of the Main view as follows (<em>app/view/phone/Main.js</em>):</p>\n\n<pre><code><a href=\"#!/api/Ext-method-define\" rel=\"Ext-method-define\" class=\"docClass\">Ext.define</a>('Mail.view.phone.Main', {\n    extend: 'Mail.view.Main',\n\n    config: {\n        title: 'Phone-specific version',\n\n        items: [\n            {\n                xtype: 'button',\n                text: 'This is a phone...'\n            }\n        ]\n    }\n});\n</code></pre>\n\n<h3 id='profiles-section-sharing-sub-views'>Sharing Sub-Views</h3>\n\n<p>While the previous example is useful, it is more common to share certain pieces of views and stitch them together in different ways for different profiles. For example, imagine an email app where the tablet UI is a split screen with a message list on the left and the current message loaded on the right. The Phone version is the exact same message list and a similar message view, but this time in a <a href=\"#!/api/Ext.layout.Card\" rel=\"Ext.layout.Card\" class=\"docClass\">card layout</a>, since there is not enough screen space to display both views simultaneously.</p>\n\n<p>To achieve this we have to create the two shared sub-views - the message list and the message viewer. In each case we have left the class config out for brevity:</p>\n\n<pre><code><a href=\"#!/api/Ext-method-define\" rel=\"Ext-method-define\" class=\"docClass\">Ext.define</a>('Mail.view.MessageList', {\n    extend: '<a href=\"#!/api/Ext.dataview.List\" rel=\"Ext.dataview.List\" class=\"docClass\">Ext.List</a>',\n    xtype: 'messagelist'\n\n    // config goes here...\n});\n</code></pre>\n\n<p>And the Message Viewer:</p>\n\n<pre><code><a href=\"#!/api/Ext-method-define\" rel=\"Ext-method-define\" class=\"docClass\">Ext.define</a>('Mail.view.MessageViewer', {\n    extend: '<a href=\"#!/api/Ext.Panel\" rel=\"Ext.Panel\" class=\"docClass\">Ext.Panel</a>',\n    xtype: 'messageviewer'\n\n    // config goes here...\n});\n</code></pre>\n\n<p>Eventually, in order to achieve the target layout that contains two views, the tablet Main view might use the following code :</p>\n\n<pre><code><a href=\"#!/api/Ext-method-define\" rel=\"Ext-method-define\" class=\"docClass\">Ext.define</a>('Mail.view.tablet.Main', {\n    extend: '<a href=\"#!/api/Ext.Container\" rel=\"Ext.Container\" class=\"docClass\">Ext.Container</a>',\n\n    config: {\n        layout: 'fit',\n        items: [\n            {\n                xtype: 'messagelist',\n                width: 200,\n                docked: 'left'\n            },\n            {\n                xtype: 'messageviewer'\n            }\n        ]\n    }\n});\n</code></pre>\n\n<p>This creates a 200px wide messagelist on the left, and uses the rest of the device's screen space to show the message viewer. Now let us assume we wanted to achieve the Phone layout:</p>\n\n<pre><code><a href=\"#!/api/Ext-method-define\" rel=\"Ext-method-define\" class=\"docClass\">Ext.define</a>('Mail.view.phone.Main', {\n    extend: '<a href=\"#!/api/Ext.Container\" rel=\"Ext.Container\" class=\"docClass\">Ext.Container</a>',\n\n    config: {\n        layout: 'card',\n        items: [\n            {\n                xtype: 'messagelist'\n            },\n            {\n                xtype: 'messageviewer'\n            }\n        ]\n    }\n});\n</code></pre>\n\n<p>In this case we use a Container with a <a href=\"#!/api/Ext.layout.Card\" rel=\"Ext.layout.Card\" class=\"docClass\">card layout</a> (a layout that only shows one item at a time), and put both the list and the viewer into it. In this case we still need to add in some logic that tells the Container to show the messageviewer when a message in the list is tapped on, but we have easily reused our two sub-views in different configurations based on the currently loaded Profile.</p>\n\n<p>Similar to the previous example, we have the option to customize the two shared views for each Profile - for example we could create the <em>Mail.view.phone.MessageViewer</em> and the <em>Mail.view.tablet.MessageViewer</em> subclasses, both of which extend the <em>Mail.view.MessageViewer</em> superclass. This enables us to again share a lot of view code between those classes, while presenting customizations appropriate for the actual device used.</p>\n\n<h2 id='profiles-section-specializing-controllers'>Specializing Controllers</h2>\n\n<p>Similar to Views, many applications have some Controller logic that can be shared across multiple Profiles. In the case of Profiles, the most important differences are usually workflow-related. For example, an app's tablet profile may allow you to complete a workflow on a single page, whereas the phone profile presents a multi-page wizard.</p>\n\n<p>In the following example we have a simple Phone profile that loads a view called <em>Main</em> and a controller called <em>Messages</em>. As before, this app loads <em>app/view/phone/Main.js</em> and <em>app/controller/phone/Messages.js</em>:</p>\n\n<pre><code><a href=\"#!/api/Ext-method-define\" rel=\"Ext-method-define\" class=\"docClass\">Ext.define</a>('Mail.profile.Phone', {\n    extend: '<a href=\"#!/api/Ext.app.Profile\" rel=\"Ext.app.Profile\" class=\"docClass\">Ext.app.Profile</a>',\n\n    config: {\n        views: ['Main'],\n        controllers: ['Messages']\n    },\n\n    launch: function() {\n        <a href=\"#!/api/Ext-method-create\" rel=\"Ext-method-create\" class=\"docClass\">Ext.create</a>('Mail.view.phone.Main');\n    }\n});\n</code></pre>\n\n<p>Since we already know that our phone and tablet-specific controllers share most of their functionality, we create a controller superclass in <em>app/controller/Messages.js</em>:</p>\n\n<pre><code><a href=\"#!/api/Ext-method-define\" rel=\"Ext-method-define\" class=\"docClass\">Ext.define</a>('Mail.controller.Messages', {\n    extend: '<a href=\"#!/api/Ext.app.Controller\" rel=\"Ext.app.Controller\" class=\"docClass\">Ext.app.Controller</a>',\n\n    config: {\n        refs: {\n            viewer: 'messageviewer',\n            messageList: 'messagelist'\n        },\n        control: {\n            messageList: {\n                itemtap: 'loadMessage'\n            }\n        }\n    },\n\n    loadMessage: function(item) {\n        this.getViewer().load(item);\n    }\n});\n</code></pre>\n\n<p>The Controller from the previous example performs three operations:</p>\n\n<ol>\n<li>Sets up <a href=\"#!/api/Ext.app.Controller-cfg-refs\" rel=\"Ext.app.Controller-cfg-refs\" class=\"docClass\">refs</a> referencing views that we care about.</li>\n<li>Listens for the <code>itemtap</code> event on the message list and calls the <code>loadMessage()</code> function when <code>itemtap</code> is fired.</li>\n<li>Loads the selected message item into the Viewer when <code>loadMessage()</code> is called.</li>\n</ol>\n\n\n<p>We can now create the phone-specific Controller as follows:</p>\n\n<pre><code><a href=\"#!/api/Ext-method-define\" rel=\"Ext-method-define\" class=\"docClass\">Ext.define</a>('Mail.controller.phone.Messages', {\n    extend: 'Mail.controller.Messages',\n\n    config: {\n        refs: {\n            main: '#mainPanel'\n        }\n    },\n\n    loadMessage: function(item) {\n        this.callParent(arguments);\n        this.getMain().setActiveItem(1);\n    }\n});\n</code></pre>\n\n<p>This code extends the Messages superclass controller and provides the following functionality:</p>\n\n<ol>\n<li>Adds another ref for the phone UI's main panel</li>\n<li>Extends the <code>loadMessage</code> function to perform the original logic and then sets the main panel's active item to the message viewer.</li>\n</ol>\n\n\n<p>The entire superclass configuration is inherited by the subclass that extends it. In the case of duplicated configs such as <a href=\"#!/api/Ext.app.Controller-cfg-refs\" rel=\"Ext.app.Controller-cfg-refs\" class=\"docClass\">refs</a>, the configuration is merged, so the phone Messages controller class has three refs - main, viewer, and messageList. As with any class that extends another class, we can use <code>callParent</code> to extend an existing function in the superclass.</p>\n\n<p>Remember that the <em>Mail.controller.Messages</em> superclass is not declared as a dependency by either the Application or the Profile. It it automatically loaded because our <em>Mail.controller.phone.Messages</em> controller extends it.</p>\n\n<h3 id='profiles-section-what-code-to-share'>What Code to Share</h3>\n\n<p>In the previous example we were able to share some (but not all) of our <a href=\"#!/api/Ext.app.Controller-cfg-refs\" rel=\"Ext.app.Controller-cfg-refs\" class=\"docClass\">refs</a>. We were also able to share the single event that we listen for by using the Controller's <a href=\"#!/api/Ext.app.Controller-cfg-control\" rel=\"Ext.app.Controller-cfg-control\" class=\"docClass\">control</a> config. Generally speaking, the more an app diverges between profiles, the fewer refs and control configs you will be able to share.</p>\n\n<p>One Controller config that should be shared across profiles is <a href=\"#!/api/Ext.app.Controller-cfg-routes\" rel=\"Ext.app.Controller-cfg-routes\" class=\"docClass\">routes</a>. These config maps URLs to Controller actions and provides back button support and deep linking. It is important to keep the routes config in the superclass, because the same URL should map to the same content regardless of the device.</p>\n\n<p>For example, if a friend is using the phone version of your app and sends you a link to the app page she is currently on, you should be able to tap that link on your tablet device and see the tablet-specific view for that URL. Keeping all routes in the superclass enables you to keep a consistent URL structure that works regardless of device.</p>\n\n<h2 id='profiles-section-specializing-models'>Specializing Models</h2>\n\n<p>Models are customized per Profile less frequently than Controllers and Views, so that do not usually require a subclass. In this case we only specify the fully qualified class names for models:</p>\n\n<pre><code><a href=\"#!/api/Ext-method-define\" rel=\"Ext-method-define\" class=\"docClass\">Ext.define</a>('Mail.profile.Tablet', {\n    extend: '<a href=\"#!/api/Ext.app.Profile\" rel=\"Ext.app.Profile\" class=\"docClass\">Ext.app.Profile</a>',\n\n    config: {\n        models: ['Mail.model.Group']\n    }\n});\n</code></pre>\n","title":"Device Profiles"});